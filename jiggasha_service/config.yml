retrieval:
  top_k: 10
  embedding_dim: 4096

agent:
  max_tao_steps: 5
  loop_stop_threshold: 0.8
  min_relevant_passages: 3

reranker:
  threshold: 0.5
  max_batch_size: 20
  max_tokens: 1

fact_check:
  claim_extraction_temp: 0.0
  verification_temp: 0.0
